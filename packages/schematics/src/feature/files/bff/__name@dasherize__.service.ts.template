import { Injectable } from '@nestjs/common';
import type { Get<%= classify(name) %>UseCase } from './port/in/get-<%= dasherize(name) %>.use-case';
import type { <%= classify(name) %>Query } from './dto/<%= dasherize(name) %>-query.dto';
import type { <%= classify(name) %>Response } from './dto/<%= dasherize(name) %>.response';

@Injectable()
export class <%= classify(name) %>Service implements Get<%= classify(name) %>UseCase {
  // Inyectar aca el cliente del upstream, que vive en src/upstream/.
  constructor() {}

  list(_query: <%= classify(name) %>Query): Promise<<%= classify(name) %>Response[]> {
    return Promise.resolve([]);
  }
}
