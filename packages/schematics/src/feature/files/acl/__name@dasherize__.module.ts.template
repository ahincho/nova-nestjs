import { Module } from '@nestjs/common';
import { <%= classify(name) %>Controller } from './adapter/in/web/<%= dasherize(name) %>.controller';
import { <%= classify(name) %>Service } from './service/<%= dasherize(name) %>.service';

@Module({
  controllers: [<%= classify(name) %>Controller],
  providers: [
    <%= classify(name) %>Service,
    // El adaptador de salida se registra contra FIND_<%= underscore(name).toUpperCase() %>_PORT
    // y no contra su clase: es lo que deja cambiarlo por un doble en un test
    // sin tocar el servicio.
    //
    // {
    //   provide: FIND_<%= underscore(name).toUpperCase() %>_PORT,
    //   useClass: <%= classify(name) %>ClientAdapter,
    // },
  ],
})
export class <%= classify(name) %>Module {}
