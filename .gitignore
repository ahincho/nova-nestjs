node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.local

# Copia efímera del servicio de ejemplo que deja el chequeo de consumidor, y los
# tarballs que se le instalan. El servicio que se genera para probar el
# generador no está acá a propósito: se arma fuera del repositorio, porque este
# mismo archivo alcanzaría a oxlint y lo dejaría sin nada que mirar.
.consumer/
.tarballs/
